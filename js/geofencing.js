// geofencing.js
// Geofencing e alertas de zona
export function setGeofence(center, radiusKm) {
  // Salva zona no localStorage ou Supabase
}

export function checkUserInZone(userLocation, geofence) {
  // Retorna true se usuário está dentro do raio
}
